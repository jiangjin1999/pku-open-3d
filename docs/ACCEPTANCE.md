# 第一版验收记录

2026-09-23。所有合成测试使用临时数据库与明确标记的测试图片，不写入生产任务或伪装成真实校园资料。

## 已执行

| 检查 | 结果 |
| --- | --- |
| 上游资源复现 | 固定 ccbdcadd… / v1.2.0，140 个文件校验一致，原 72 MB 压缩场景构建成功 |
| 原渲染器回归 | 43 项测试通过 |
| 完整校园适配 | Chromium 加载 649,485 个原场景实例；添加/移除测试部位不丢失原对象，WebGL 无错误，建筑反馈带上视角和截图 |
| 社区 API、权限与发布状态 | 27 项测试通过 |
| 桌面与手机首页 | 三个入口可辨识，390 px 宽无横向溢出 |
| 手机匿名照片 | 新地点、楼层、时间未知、拍摄位置、用途授权、上传与凭证完成 |
| 资料访问 | 匿名和其他任务认领者均无法下载；撤回后停止访问并删除在线照片 |
| 独立反馈与上下文 | 不要求照片；预览生成的截图、圈画、地点和版本可一起保留 |
| 轻量页面 | 首页、上传和反馈没有请求完整校园场景 |
| 局部空间 | 合成四楼部位实际经 Chromium 渲染并生成预览，不生成其他楼层 |
| 发布逻辑 | 模拟 GitHub 的不可变树、PR、exact-head 合并；成功切换版本，越界文件与 head 改变被阻止 |
| 失败与幂等 | 渲染失败保留旧模型；重提同样内容返回同一记录；资料修改或撤回使候选失效 |
| 版本链和回退 | 新反馈形成后续版本，维护者可回退并暂停发布；旧版本仍可查询 |
| 备份恢复 | 有照片的隔离数据库完成哈希、SQLite 完整性和恢复检查；生产每日备份与恢复演练成功 |
| 运行与重启 | L40s 用户级 web/worker 运行，服务重启后健康检查通过 |
| 资源约束 | 单 worker；4 核 CPU 亲和性、16 GiB 内核内存上限；照片配额默认 50 GB |

浏览器检查可重复运行 `node scripts/smoke-community.mjs`，只写临时数据。完整校园及适配器检查使用 `node scripts/check-campus.mjs`，合成几何仅注入测试浏览器，不写生产服务。截图与机器日志保存在忽略的 `output/playwright/`，不提交公开仓库。

## 需要外部授权后继续验收

- 用户注册并接入 `pku-open3d.org`，授权 Cloudflare 命名隧道。当前没有宣称固定域名已公开可用。
- 在 `/setup/` 创建并安装本项目 GitHub App，验证真实 GitHub 登录、CLI 配对及仓库权限。
- 用明确标记的真实测试提交完成 **实际 GitHub PR → 自动合并 → 网站发布**。当前自动发布逻辑已测试，但模拟 GitHub 不等于实际账号验收。
- 从校外网络验证 HTTPS、手机匿名提交、资料领取和发布结果。
- 用户提供王选所实图与确认位置，再完成首次真实建模，不以合成测试替代。
